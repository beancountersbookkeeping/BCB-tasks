<div class="container"> 
<h2>Companies</h2>

<% @companies.each do |company| %>
  <li><h3><%= link_to company.name.capitalize, company_path(company.id) %></h2></li>
<% end %>
</br>
<%= link_to "New Company", new_company_path, :class => 'btn btn-primary' %>

</div>
