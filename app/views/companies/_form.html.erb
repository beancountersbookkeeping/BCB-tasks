<%= form_for @company do |f| %>
  <%= form_tag(company[:name]) do %>
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control', placeholder: "Enter name" %>
  <% end %>
  <%= f.submit "Save", class: 'btn btn-success' %>
<% end %>
